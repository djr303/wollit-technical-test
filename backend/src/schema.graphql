type Query {
  categories: [Category!]
}

# type Mutation {
#  addItem(parentCategoryID: ID!): Item
#  updateItem(itemID: ID!): Item
#  addCategory(parentCategoryID: ID!): Category
#  updateCategory(id: ID, category: Category): Category
# }

type Item {
  id: ID!
  name: String!
}

type Category {
  id: ID!
  name: String!
  items: [Item]
  categories: [Category]
}
